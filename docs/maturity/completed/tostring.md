# `tostring`

**Category:** Number / Universal  
**Input type:** Number (specific entry) + Any (universal entry)

## What's implemented

Two catalog entries: one `InputType::Number` (converts a number to its string representation) and one `InputType::Any`. The number-specific entry surfaces first when a number flows in. No argument needed.

## What's missing

Nothing. Zero-argument transform.

## Estimated complexity

N/A — already complete.

## Needs changes to overall logic

No.
