# `from_entries`

**Category:** Array / Object  
**Input type:** Array (of `{key, value}` objects) — also covered by the Object `to_entries`/`with_entries` group

## What's implemented

Type-aware catalog entry (`InputType::Array`). No argument needed; converts `[{key, value}]` to an object.

## What's missing

Nothing. Zero-argument transform.

## Estimated complexity

N/A — already complete.

## Needs changes to overall logic

No.
