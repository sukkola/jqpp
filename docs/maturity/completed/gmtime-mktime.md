# `gmtime` / `mktime`

**Category:** Number / Array  
**Input type:** Number (gmtime) / Array (mktime — broken-down time array)

## What's implemented

Type-aware catalog entries. `gmtime` takes a UNIX timestamp and returns the broken-down time array; `mktime` is the inverse. No argument needed for either.

## What's missing

Nothing. Zero-argument transforms.

## Estimated complexity

N/A — already complete.

## Needs changes to overall logic

No.
