# `empty`

**Category:** Universal  
**Input type:** Any

## What's implemented

`InputType::Any` catalog entry. Insert text is bare `empty`. Produces no output (zero results).

## What's missing

Nothing. Zero-argument; used to suppress output or short-circuit in `if`/`try`.

## Estimated complexity

N/A — already complete.

## Needs changes to overall logic

No.
