# `null` / `true` / `false`

**Category:** Universal  
**Input type:** Any (literal generators, ignore input)

## What's implemented

`InputType::Any` catalog entries. All three are literal value generators — they produce a fixed value regardless of input. Insert text is the bare literal name.

## What's missing

Nothing. Literal completions.

## Estimated complexity

N/A — already complete.

## Needs changes to overall logic

No.
