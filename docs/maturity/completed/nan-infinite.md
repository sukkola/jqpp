# `nan` / `infinite`

**Category:** Number / Universal  
**Input type:** Any (they generate a value, not transform one)

## What's implemented

Catalog entries with `InputType::Any`. Insert text is the bare literal. These are generator expressions, not input-dependent transforms.

## What's missing

Nothing. Zero-argument value generators.

## Estimated complexity

N/A — already complete.

## Needs changes to overall logic

No.
