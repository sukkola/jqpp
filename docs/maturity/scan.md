# `scan`

**Category:** String  
**Input type:** String

## What's implemented

Type-aware catalog entry. Insert text `scan("")`.

## What's missing

Regex argument not completed. Same situation as `test` — literal-value suggestions from data are theoretically possible.

## Estimated complexity

`Medium` — same path as `test`.

## Needs changes to overall logic

Yes — same as `test`.
